<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.familia.monitor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:label="Servicio de Sincronización"
        android:allowBackup="false"
        android:theme="@style/Theme.FamiliaMonitor">

        <!-- Nueva entrada discreta con PIN -->
        <activity
            android:name=".PinActivity"
            android:exported="true"
            android:label="Ajustes de Sincronización">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ConsentActivity"
            android:exported="false"
            android:label="Configuración de Servicio" />

        <activity
            android:name=".StatusActivity"
            android:exported="false"
            android:label="Estado de Sincronización" />

        <!-- Receptor de Administrador para bloqueo de desinstalación -->
        <receiver
            android:name=".AdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- Requiere que el usuario active manualmente el acceso a notificaciones -->
        <service
            android:name=".NotificationCaptureService"
            android:exported="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:label="Servicio de Sincronización">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".ForegroundStatusService"
            android:exported="false"
            android:label="Servicio de Sincronización"
            android:foregroundServiceType="dataSync" />

    </application>
</manifest>
